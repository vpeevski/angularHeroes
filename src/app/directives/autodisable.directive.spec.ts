import { AutoDisableDirective } from './auto-disable.directive';

describe('AutoDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoDisableDirective(null);
    expect(directive).toBeTruthy();
  });
});
